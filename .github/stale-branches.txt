# Branches to delete, one per line. Blank lines and # comments ignored.
#
# Re-audited 2026-08-22 against `git ls-remote --heads origin`. The previous
# list named claude/routine-backend-upkeep, which no longer exists — it was
# deleted after that audit and the list was never updated. A list that names
# only phantoms reports "0 to delete" and reads as "nothing to clean up",
# which is how the branch below survived.
#
# The remote now holds exactly three branches:
#   main                                 the default branch
#   gh-pages                             deployment target, NEVER delete
#   claude/routine-count-drift-20260821  listed below
#
# claude/routine-count-drift-20260821 is the last of the abandoned Routine
# branches described in .claude/rules/testing.md: a scheduled run that pushed
# a branch and never opened a PR.
#
# Checked before listing it, per concern rather than per branch:
#   * Its count-drift fixes are superseded — check-counts.py passes on main.
#   * Its OTHER half was NOT superseded. It carried the GA4 tag for 16 live,
#     indexed pages (ASER, NFHS, UDISE, PLFS, AISHE and 11 more) that had no
#     analytics at all. That was salvaged onto main in PR #991 before listing
#     the branch here — deleting it first would have destroyed real work.
#   * `comm` against main's tree confirms 0 files exist only on this branch.
#
# Nothing on it is now unique. Safe to delete.
claude/routine-count-drift-20260821
