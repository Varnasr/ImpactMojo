{
  "name": "impactmojo-visual-regression",
  "version": "1.0.0",
  "private": true,
  "description": "Visual-regression harness gating the CSS @layer migration (#563).",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "baseline": "playwright test --update-snapshots",
    "report": "playwright show-report report"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1"
  }
}
